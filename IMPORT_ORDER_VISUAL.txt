╔═══════════════════════════════════════════════════════════════════════╗
║                    DATABRICKS NOTEBOOK IMPORT ORDER                    ║
║                  ⚠️  CRITICAL: READ THIS CAREFULLY ⚠️                   ║
╚═══════════════════════════════════════════════════════════════════════╝

┌───────────────────────────────────────────────────────────────────────┐
│                         ❌ WRONG (CAUSES ERRORS)                       │
└───────────────────────────────────────────────────────────────────────┘

# COMMAND ----------
%pip install pyyaml requests pandas
# COMMAND ----------

from config.settings import config      ← ❌ WRONG! Importing first
from utils.api_client import OpenF1Client
import sys
dbutils.library.restartPython()         ← 💥 This KILLS the kernel!

# COMMAND ----------

print(config.catalog)  ← ❌ ERROR: Kernel unresponsive or NameError!

┌─────────────────────────────────────────────────────────────────────────┐
│  What Happens:                                                           │
│  1. ❌ You import config, sys, etc.                                      │
│  2. 💥 restartPython() TERMINATES the Python kernel                      │
│  3. 🔄 Kernel restarts (but imports are GONE)                            │
│  4. 💀 Code tries to continue but kernel is BROKEN                       │
│  5. 🚨 Result: "Fatal error: The Python kernel is unresponsive"         │
└─────────────────────────────────────────────────────────────────────────┘


┌───────────────────────────────────────────────────────────────────────┐
│                      ✅ CORRECT (WORKS PERFECTLY)                      │
└───────────────────────────────────────────────────────────────────────┘

# COMMAND ----------
%pip install pyyaml requests pandas
# COMMAND ----------

# ⚠️ NO IMPORTS BEFORE THIS LINE! ⚠️
dbutils.library.restartPython()         ← ✅ Alone in its own cell!

# COMMAND ----------

import sys                              ← ✅ NOW we import (after restart)
import os
from config.settings import config
from utils.api_client import OpenF1Client

# COMMAND ----------

print(config.catalog)  ← ✅ Works perfectly!

┌─────────────────────────────────────────────────────────────────────────┐
│  What Happens:                                                           │
│  1. ✅ Install packages with %pip                                        │
│  2. 🔄 Restart Python (loads packages, clears everything)                │
│  3. ✅ Import modules (pyyaml is now available!)                         │
│  4. ✅ Use imports (everything works!)                                   │
└─────────────────────────────────────────────────────────────────────────┘


╔═══════════════════════════════════════════════════════════════════════╗
║                            THE GOLDEN RULE                             ║
╚═══════════════════════════════════════════════════════════════════════╝

    restartPython() MUST be in its OWN CELL with NO imports before it

                        Cell 1:  %pip install
                        Cell 2:  dbutils.library.restartPython()  ← ALONE!
                        Cell 3:  All imports go here


╔═══════════════════════════════════════════════════════════════════════╗
║                            VISUAL TIMELINE                             ║
╚═══════════════════════════════════════════════════════════════════════╝

❌ WRONG ORDER (Kernel Dies):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Time: 0ms        Import config ✓
Time: 100ms      Import sys ✓
Time: 200ms      Call restartPython()
Time: 300ms      💥 KERNEL TERMINATED 💥
Time: 400ms      🔄 Kernel restarting...
Time: 500ms      💀 Try to use config → ERROR! It's gone!
Time: 600ms      🚨 Kernel unresponsive/broken
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ CORRECT ORDER (Everything Works):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Time: 0ms        Call restartPython()
Time: 100ms      🔄 Kernel restarting...
Time: 200ms      ✅ Kernel ready with pyyaml loaded
Time: 300ms      Import config ✓
Time: 400ms      Import sys ✓
Time: 500ms      Use config ✓
Time: 600ms      ✅ Everything working perfectly!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


╔═══════════════════════════════════════════════════════════════════════╗
║                          MEMORY ANALOGY                                ║
╚═══════════════════════════════════════════════════════════════════════╝

Think of restartPython() like RESTARTING YOUR COMPUTER:

❌ WRONG:
    1. Open Microsoft Word
    2. Type "Hello World" in the document
    3. Restart your computer          ← Word closes! Document gone!
    4. Try to continue typing         ← ERROR! Word isn't even open!

✅ CORRECT:
    1. Restart your computer          ← Fresh start
    2. Open Microsoft Word            ← Now it's available
    3. Type "Hello World"             ← Works perfectly!


╔═══════════════════════════════════════════════════════════════════════╗
║                      YOUR NOTEBOOKS (FIXED)                            ║
╚═══════════════════════════════════════════════════════════════════════╝

All notebooks now have clear ⚠️ WARNING comments:

✅ notebooks/01_ingest_f1_data.py
✅ notebooks/01_ingest_f1_data_incremental.py
✅ notebooks/01_ingest_f1_data_incremental_FIXED.py  ← RECOMMENDED

They all have this structure:

    # COMMAND ----------
    %pip install pyyaml requests pandas
    
    # COMMAND ----------
    # ⚠️ WARNING: DO NOT ADD IMPORTS BEFORE THIS LINE! ⚠️
    dbutils.library.restartPython()
    
    # COMMAND ----------
    # ✅ NOW we can import (after restart)
    import sys
    from config.settings import config


╔═══════════════════════════════════════════════════════════════════════╗
║                         PLEASE REMEMBER                                ║
╚═══════════════════════════════════════════════════════════════════════╝

DO NOT MOVE IMPORTS BEFORE restartPython()!

Every time you do:
    ❌ Kernel becomes unresponsive
    ❌ You get NameError  
    ❌ Notebook crashes
    ❌ You waste time debugging

The notebooks are FIXED. Leave them as-is!

If your IDE tries to "auto-format" and move imports to the top:
    → Undo it immediately!
    → Imports MUST stay AFTER restartPython()


╔═══════════════════════════════════════════════════════════════════════╗
║                         QUICK CHECKLIST                                ║
╚═══════════════════════════════════════════════════════════════════════╝

Before running your notebook:

[ ] Cell 1: %pip install pyyaml requests pandas
[ ] Cell 2: ONLY dbutils.library.restartPython() (no imports!)
[ ] Cell 3: All imports (after restart)
[ ] No imports before restartPython()
[ ] Workspace path is correct

If any checkbox is ✗, fix it before running!


╔═══════════════════════════════════════════════════════════════════════╗
║                     RUN THIS NOTEBOOK NOW                              ║
╚═══════════════════════════════════════════════════════════════════════╝

RECOMMENDED: 01_ingest_f1_data_incremental_FIXED.py

Why?
  ✅ Correct import order (no kernel issues)
  ✅ Memory-efficient (no crashes)  
  ✅ Handles large datasets
  ✅ Progress logging
  ✅ Production-ready

Just update line 28 with your workspace path and run!

